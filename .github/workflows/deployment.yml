name: CI with Deployment Badge

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Echo deployment info
        run: echo "Pretend deploying to Hugging Face Spaces..."

      - name: Show deployment URL
        run: echo "Visit https://huggingface.co/spaces/KikKoh/Hokkien"
